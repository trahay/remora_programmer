<!DOCTYPE html>
<% include ../partials/head %>
<body id="home">

  <!-- header area -->
  <header class="wrapper clearfix">
    <div id="banner">
      <div id="logo"><a href="/"><img src="styles/images/heater.png" alt="logo"></a></div> 
    </div>

    <!-- main navigation -->
    <nav id="topnav" role="navigation">
      <div class="menu-toggle">Menu</div>  
      <ul class="srt-menu" id="menu-main-navigation">
        <li><a href="/">Home</a></li>
	<li class="current"><a href="afficher_zones">Zones</a></li>
	<li><a href="afficher_programs">Prog journée</a></li>
	<li><a href="program_semaine">Prog semaine</a></li>
	<li><a href="logs">Logs</a></li>
      </ul>
    </nav><!-- #topnav -->
  </header><!-- end header -->

  <section id="page-header" class="clearfix">    
    <!-- responsive FlexSlider image slideshow -->
    <div class="wrapper">
      <h1>Zones</h1>
    </div>
  </section>

  <!-- main content area -->
  <div class="wrapper" id="main">
    <!-- content area -->
    <section id="content">

    <p>Voici les <% zones.length %> zones:
    </p>
      <% for(var i=0; i< zones.length; i++){ %>
      <div>
      <a href="edit_zones?id=<%= zones[i]._id %>"><img src="styles/images/edit.png"></a>
	<a href="supprime_zone?id=<%= zones[i]._id %>"><img src="styles/images/cancel.png"></a>
	<%= zones[i].name %>
	(<%= zones[i].pin1 %>, <%= zones[i].pin2 %>)
	<a href="edit_prog_sem?id=<%= zones[i].program %>"><%= zones[i].program %></a>
	</div>
      <% } %>

    <h1> Ajouter une zone</h1>
    <form method="post" action="ajouter_zone">
      <input type="hidden" value="<%= selected_zone._id %>">
      Zone Name: <input type="text" name="name"
			<% if(typeof selected_zone != 'undefined' && selected_zone) { %>
			value="<%= selected_zone.name %>"
			<% } %>
			>
      <br>
      PIN1: <input type="text" name="pin1"
			<% if(typeof selected_zone != 'undefined' && selected_zone) { %>
			value="<%= selected_zone.pin1 %>"
			<% } %>
		   ><br>
      PIN2: <input type="text" name="pin2"
		   <% if(typeof selected_zone != 'undefined' && selected_zone) { %>
		   value="<%= selected_zone.pin2 %>"
		   <% } %>
		   ><br>
      Program:

      <select name="program">
	<% for(var i=0; i<programs.length; i++) { %>
	<option value="<%= programs[i]._id %>"
		<% if(typeof selected_zone != 'undefined' && selected_zone) {
		   if(selected_zone.program == programs[i]._id) {
		   %>
		selected
		<% }
		   } %>
		><%= programs[i].name %></option>
	<%    } %>
      </select><br/>
      <input type="submit" value="Submit">
    </form>

    </section><!-- #end content area -->

  </div><!-- #end div #main .wrapper -->
  <% include ../partials/footer %>
</body>
</html>
